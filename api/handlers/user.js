const Boom = require('Boom');
const uuid = require('node-uuid');

module.exports = {
  register: (request, reply) => {
    reply(Boom.notImplemented());
  },
  login: (request, reply) => {
    reply(Boom.notImplemented());
  }
}
